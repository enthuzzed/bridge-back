export enum TableNameEnum {
  TRANSACTION = 'transaction',
  ICP_TRANSACTION = 'icp_transaction'
}

export enum BlockchainTypeEnum {
  DFINITY = 'dfinity',
  POLYGON = 'polygon',
}
export enum TransactionStateEnum {
  IN_PROGRESS = 'in_progress',
  CANCELED = 'canceled',
  COMPLETED = 'completed',
}
